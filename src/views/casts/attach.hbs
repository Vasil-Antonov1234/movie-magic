            <section id="attachCast">
                <div class="attachCastSection">
                <h1>Attach New Cast</h1>
                <div class="attach-form">
                    <h2>{{movie.title}}</h2>
                    <img class="movie" src="{{movie.imageUrl}}">
                    <form action="" method="">
                        <label for="cast">Cast</label>
                        <select id="cast" name="cast">
                        
                        {{#each casts}}
                            <option value="{{casts.name}}">{{casts.name}}</option>
                        {{else}}
                        <div class="no-posts">
                            <p class="no-offer">There are no cast yet...</p>
                        </div>
                        {{/each}}
                        </select>
                        <div id="center-btn">
                            <button id="create-btn">Add Cast</button>
                        </div>
                    </form>
                </div>
            </div>
            </section>